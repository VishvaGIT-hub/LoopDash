
---

## 📁 CSS FILES

### **css/main.css** (Continue in next message due to length...)

Since this is extremely long, let me provide the CSS files:

### **css/main.css**

```css
/* ============================================
   LOOP DASH ULTRA 3D - MAIN STYLES
   ============================================ */

:root {
    --primary: #00f2fe;
    --secondary: #4facfe;
    --accent: #f093fb;
    --danger: #ff6b6b;
    --success: #51cf66;
    --warning: #ffd93d;
    
    --dark-1: #0a0a0f;
    --dark-2: #141420;
    --dark-3: #1e1e30;
    
    --glow-primary: 0 0 20px rgba(0, 242, 254, 0.6);
    --glow-accent: 0 0 20px rgba(240, 147, 251, 0.6);
    
    --font-primary: 'Orbitron', monospace;
    --font-secondary: 'Rajdhani', sans-serif;
    
    --transition-fast: 0.15s ease;
    --transition-base: 0.3s ease;
    --transition-slow: 0.5s ease;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html, body {
    width: 100%;
    height: 100%;
    overflow: hidden;
    font-family: var(--font-secondary);
    background: var(--dark-1);
    color: #fff;
    user-select: none;
    -webkit-user-select: none;
    -webkit-tap-highlight-color: transparent;
}

canvas {
    display: block;
}

/* ============ LOADER ============ */
.loader {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: none;
    align-items: center;
    justify-content: center;
    z-index: 10000;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.loader.active {
    display: flex;
}

.loader-background {
    position: absolute;
    width: 100%;
    height: 100%;
}

#loaderCanvas {
    width: 100%;
    height: 100%;
}

.loader-content {
    position: relative;
    z-index: 1;
    text-align: center;
    padding: 2rem;
}

.game-logo {
    margin-bottom: 3rem;
}

.logo-text {
    font-family: var(--font-primary);
    font-size: clamp(2rem, 8vw, 5rem);
    font-weight: 900;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.text-3d {
    position: relative;
    background: linear-gradient(45deg, var(--primary), var(--accent));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    text-shadow: 0 0 30px rgba(0, 242, 254, 0.5);
    animation: float 3s ease-in-out infinite;
}

@keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
}

.text-ultra {
    font-size: 0.5em;
    color: rgba(255, 255, 255, 0.9);
}

.progress-container {
    max-width: 500px;
    margin: 0 auto;
}

.progress-bar {
    width: 100%;
    height: 10px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 10px;
    overflow: hidden;
    position: relative;
    margin-bottom: 1.5rem;
}

.progress-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--primary), var(--accent));
    width: 0%;
    transition: width 0.3s ease;
    box-shadow: var(--glow-primary);
}

.progress-glow {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
    animation: shimmer 2s infinite;
}

@keyframes shimmer {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(100%); }
}

.loading-info {
    display: flex;
    justify-content: space-between;
    margin-bottom: 1rem;
    font-size: 1rem;
}

.loading-percent {
    font-weight: 700;
    color: var(--primary);
}

.loading-tip {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.8);
}

/* ============ SCREENS ============ */
.screen {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: none;
    overflow: hidden;
}

.screen.active {
    display: block;
}

/* ============ MAIN MENU ============ */
#mainMenu {
    overflow-y: auto;
}

#menuBackground {
    position: absolute;
    width: 100%;
    height: 100%;
    z-index: 0;
}

.menu-container {
    position: relative;
    z-index: 1;
    max-width: 1400px;
    margin: 0 auto;
    padding: 1rem;
    padding-bottom: 6rem;
}

/* Menu Header */
.menu-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 2rem;
    flex-wrap: wrap;
}

.player-card {
    display: flex;
    gap: 1rem;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(20px);
    padding: 1rem;
    border-radius: 15px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    flex: 1;
    min-width: 250px;
}

.player-avatar {
    position: relative;
    width: 80px;
    height: 80px;
    border-radius: 50%;
    border: 3px solid var(--primary);
    overflow: hidden;
    box-shadow: var(--glow-primary);
}

.player-level {
    position: absolute;
    bottom: -5px;
    right: -5px;
    width: 30px;
    height: 30px;
    background: linear-gradient(135deg, var(--accent), var(--primary));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 900;
    font-size: 0.9rem;
    border: 2px solid var(--dark-1);
}

.player-info {
    flex: 1;
}

.player-name {
    font-family: var(--font-primary);
    font-size: 1.2rem;
    margin-bottom: 0.5rem;
    text-shadow: var(--glow-primary);
}

.xp-bar {
    width: 100%;
    height: 6px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    overflow: hidden;
    margin-bottom: 0.5rem;
}

.xp-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--success), var(--primary));
    width: 45%;
    transition: width 0.5s ease;
}

.player-rank {
    font-size: 0.85rem;
    color: var(--warning);
    font-weight: 600;
}

/* Currencies */
.currencies {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
}

.currency {
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(20px);
    padding: 0.75rem 1rem;
    border-radius: 25px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 700;
    font-family: var(--font-primary);
}

.currency .icon {
    font-size: 1.3rem;
}

.currency .amount {
    font-size: 1rem;
}

.add-btn {
    background: linear-gradient(135deg, var(--primary), var(--accent));
    border: none;
    color: #fff;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    cursor: pointer;
    font-size: 1rem;
    font-weight: 900;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: transform var(--transition-fast);
}

.add-btn:hover {
    transform: scale(1.1);
}

.settings-btn {
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 50%;
    width: 50px;
    height: 50px;
    font-size: 1.5rem;
    cursor: pointer;
    transition: all var(--transition-base);
}

.settings-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    transform: rotate(90deg);
}

/* Character Showcase */
.character-showcase {
    background: rgba(0, 0, 0, 0.4);
    backdrop-filter: blur(20px);
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 2rem;
    margin-bottom: 2rem;
    position: relative;
}

.character-canvas {
    width: 100%;
    height: 400px;
    border-radius: 15px;
    margin-bottom: 1.5rem;
}

.character-controls {
    position: absolute;
    top: 50%;
    left: 1rem;
    right: 1rem;
    transform: translateY(-50%);
    display: flex;
    justify-content: space-between;
    pointer-events: none;
}

.rotate-btn {
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: #fff;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    font-size: 1.5rem;
    cursor: pointer;
    pointer-events: all;
    transition: all var(--transition-base);
}

.rotate-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    box-shadow: var(--glow-primary);
}

/* Character Stats */
.character-stats {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-bottom: 1.5rem;
}

.stat {
    display: grid;
    grid-template-columns: auto 1fr auto auto;
    align-items: center;
    gap: 0.75rem;
}

.stat-icon {
    font-size: 1.5rem;
}

.stat-name {
    font-weight: 600;
    color: rgba(255, 255, 255, 0.7);
}

.stat-bar {
    height: 8px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    overflow: hidden;
}

.stat-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--primary), var(--accent));
    transition: width 0.5s ease;
}

.stat-value {
    font-family: var(--font-primary);
    font-weight: 700;
    color: var(--primary);
    min-width: 30px;
    text-align: right;
}

.customize-btn {
    width: 100%;
    padding: 1rem;
    background: linear-gradient(135deg, var(--accent), var(--primary));
    border: none;
    border-radius: 15px;
    color: #fff;
    font-weight: 700;
    font-size: 1rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    transition: all var(--transition-base);
    box-shadow: var(--glow-accent);
}

.customize-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 0 30px rgba(240, 147, 251, 0.8);
}

/* Main Play Button */
.btn-play {
    width: 100%;
    padding: 1.5rem;
    background: linear-gradient(135deg, #ff6b6b, #ff8e53);
    border: none;
    border-radius: 25px;
    color: #fff;
    font-family: var(--font-primary);
    font-weight: 900;
    font-size: 1.8rem;
    cursor: pointer;
    position: relative;
    overflow: hidden;
    margin-bottom: 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    box-shadow: 0 0 40px rgba(255, 107, 107, 0.6);
    transition: all var(--transition-base);
}

.btn-play:hover {
    transform: scale(1.02);
    box-shadow: 0 0 60px rgba(255, 107, 107, 0.8);
}

.btn-glow {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
    animation: btnShine 3s infinite;
}

@keyframes btnShine {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(100%); }
}

.btn-icon {
    font-size: 2rem;
}

/* Game Modes */
.game-modes {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 1rem;
    margin-bottom: 2rem;
}

.mode-btn {
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 15px;
    padding: 1.5rem 1rem;
    cursor: pointer;
    transition: all var(--transition-base);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    position: relative;
    color: #fff;
}

.mode-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: var(--primary);
    box-shadow: var(--glow-primary);
    transform: translateY(-5px);
}

.mode-icon {
    font-size: 2.5rem;
}

.mode-name {
    font-weight: 700;
}

.mode-badge {
    position: absolute;
    top: 10px;
    right: 10px;
    background: var(--danger);
    color: #fff;
    font-size: 0.7rem;
    font-weight: 700;
    padding: 0.25rem 0.5rem;
    border-radius: 10px;
    animation: badgePulse 2s infinite;
}

@keyframes badgePulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
}

/* Stats Grid */
.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: 1rem;
    margin-bottom: 2rem;
}

.stat-card {
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 15px;
    padding: 1.5rem 1rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    text-align: center;
}

.stat-card .icon {
    font-size: 2.5rem;
}

.stat-card .value {
    font-family: var(--font-primary);
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--primary);
}

.stat-card .label {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.6);
}

/* Missions Widget */
.missions-widget {
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    padding: 1.5rem;
    margin-bottom: 2rem;
}

.widget-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
}

.widget-header h3 {
    font-family: var(--font-primary);
    font-size: 1.2rem;
    color: var(--primary);
}

.timer {
    color: var(--warning);
    font-weight: 700;
}

/* Bottom Navigation */
.bottom-nav {
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
    background: rgba(0, 0, 0, 0.9);
    backdrop-filter: blur(20px);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    display: flex;
    justify-content: space-around;
    padding: 0.75rem 0.5rem;
    z-index: 100;
}

.nav-btn {
    background: transparent;
    border: none;
    color: rgba(255, 255, 255, 0.5);
    cursor: pointer;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.25rem;
    padding: 0.5rem 1rem;
    border-radius: 10px;
    transition: all var(--transition-base);
    flex: 1;
    max-width: 100px;
}

.nav-btn.active {
    color: var(--primary);
    background: rgba(0, 242, 254, 0.1);
}

.nav-btn:hover {
    color: #fff;
}

.nav-icon {
    font-size: 1.5rem;
}

.nav-label {
    font-size: 0.75rem;
    font-weight: 600;
}

/* Game Screen */
#gameCanvas {
    width: 100%;
    height: 100%;
    background: #000;
}

/* Game HUD */
.game-hud {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 10;
}

.game-hud > * {
    pointer-events: all;
}

.hud-top {
    position: absolute;
    top: 1rem;
    left: 1rem;
    right: 1rem;
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 1rem;
}

.pause-btn {
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: #fff;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    font-size: 1.5rem;
    cursor: pointer;
    transition: all var(--transition-base);
}

.pause-btn:hover {
    background: rgba(255, 255, 255, 0.1);
    box-shadow: var(--glow-primary);
}

.score-display {
    flex: 1;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 15px;
    padding: 0.75rem 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
}

.score-label {
    font-size: 0.7rem;
    color: rgba(255, 255, 255, 0.6);
    font-weight: 700;
    letter-spacing: 2px;
}

.score-value {
    font-family: var(--font-primary);
    font-size: 2.5rem;
    font-weight: 900;
    color: var(--primary);
    text-shadow: var(--glow-primary);
    line-height: 1;
}

.multiplier {
    background: linear-gradient(135deg, var(--warning), #ff6b6b);
    color: #fff;
    font-family: var(--font-primary);
    font-weight: 900;
    font-size: 0.9rem;
    padding: 0.25rem 0.75rem;
    border-radius: 15px;
    margin-top: 0.5rem;
}

.distance-display, .coins-display {
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 25px;
    padding: 0.75rem 1.25rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-primary);
    font-weight: 700;
    font-size: 1.2rem;
}

/* Combo Display */
.hud-center {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
}

.combo-display {
    text-align: center;
    opacity: 0;
    transition: opacity var(--transition-base);
}

.combo-display:not(.hidden) {
    opacity: 1;
    animation: comboAppear 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

@keyframes comboAppear {
    0% { transform: scale(0); }
    50% { transform: scale(1.2); }
    100% { transform: scale(1); }
}

.combo-number {
    font-family: var(--font-primary);
    font-size: 6rem;
    font-weight: 900;
    background: linear-gradient(135deg, var(--warning), #ff6b6b);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    line-height: 1;
    display: block;
}

.combo-text {
    font-family: var(--font-primary);
    font-size: 2rem;
    font-weight: 900;
    color: #fff;
    letter-spacing: 5px;
}

/* Touch Controls */
.touch-controls {
    position: absolute;
    bottom: 0;
    width: 100%;
    height: 35%;
    display: flex;
    pointer-events: all;
}

.touch-area {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
}

.touch-left {
    background: linear-gradient(to top, rgba(67, 233, 123, 0.15), transparent);
}

.touch-right {
    background: linear-gradient(to top, rgba(79, 172, 254, 0.15), transparent);
}

.touch-indicator {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    opacity: 0.4;
    transition: opacity var(--transition-fast);
}

.touch-area:active .touch-indicator {
    opacity: 1;
}

.touch-icon {
    font-size: 4rem;
    filter: drop-shadow(0 0 10px rgba(255, 255, 255, 0.5));
}

.touch-label {
    font-family: var(--font-primary);
    font-weight: 700;
    font-size: 1.2rem;
    letter-spacing: 2px;
}

/* Modal */
.modal {
    position: fixed;
    inset: 0;
    display: none;
    align-items: center;
    justify-content: center;
    z-index: 1000;
}

.modal.active {
    display: flex;
}

.modal-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.9);
    backdrop-filter: blur(10px);
}

.modal-content {
    position: relative;
    background: linear-gradient(135deg, #1a1a2e, #16213e);
    border-radius: 20px;
    padding: 2rem;
    max-width: 90%;
    width: 500px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    text-align: center;
    animation: modalSlideIn 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

@keyframes modalSlideIn {
    0% {
        transform: scale(0.8) translateY(50px);
        opacity: 0;
    }
    100% {
        transform: scale(1) translateY(0);
        opacity: 1;
    }
}

.modal-title {
    font-family: var(--font-primary);
    font-size: 2.5rem;
    margin-bottom: 1.5rem;
}

.rank-badge {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    padding: 1rem 2rem;
    border-radius: 15px;
    margin-bottom: 2rem;
}

.rank-letter {
    font-family: var(--font-primary);
    font-size: 3rem;
    font-weight: 900;
}

.rank-name {
    font-size: 1.2rem;
    font-weight: 700;
}

.final-stats {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-bottom: 1.5rem;
}

.final-stat {
    display: flex;
    align-items: center;
    gap: 1rem;
    background: rgba(255, 255, 255, 0.05);
    padding: 1rem;
    border-radius: 10px;
}

.final-stat .stat-icon {
    font-size: 2rem;
}

.stat-info {
    flex: 1;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.stat-label {
    font-weight: 600;
    color: rgba(255, 255, 255, 0.7);
}

.stat-value {
    font-family: var(--font-primary);
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--primary);
}

.new-record {
    background: linear-gradient(135deg, #ffd93d, #ff6b6b);
    padding: 1rem;
    border-radius: 15px;
    font-weight: 900;
    font-size: 1.2rem;
    margin-bottom: 1.5rem;
    animation: recordGlow 1s infinite;
}

@keyframes recordGlow {
    0%, 100% { box-shadow: 0 0 20px rgba(255, 215, 61, 0.5); }
    50% { box-shadow: 0 0 40px rgba(255, 215, 61, 0.8); }
}

.new-record.hidden {
    display: none;
}

.modal-actions {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

.btn-modal {
    width: 100%;
    padding: 1rem;
    border: none;
    border-radius: 15px;
    color: #fff;
    font-weight: 700;
    font-size: 1.1rem;
    cursor: pointer;
    transition: all var(--transition-base);
}

.btn-play-again {
    background: linear-gradient(135deg, var(--primary), var(--accent));
    box-shadow: var(--glow-primary);
}

.btn-play-again:hover {
    transform: translateY(-2px);
    box-shadow: 0 0 30px rgba(0, 242, 254, 0.8);
}

.btn-home {
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
}

.btn-home:hover {
    background: rgba(255, 255, 255, 0.2);
}

/* Hidden Utility */
.hidden {
    display: none !important;
}
